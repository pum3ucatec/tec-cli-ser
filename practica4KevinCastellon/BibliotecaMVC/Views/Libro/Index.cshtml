@model IEnumerable<BibliotecaMVC.Models.Libro>

@{
    ViewData["Title"] = "Biblioteca - Listado de Libros";
}

<div class="container mt-4">
    <div class="row mb-3">
        <div class="col">
            <h2>📚 Catálogo de Libros</h2>
        </div>
        <div class="col text-end">
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Agregar Libro
            </a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="card">
        <div class="card-body">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Título</th>
                        <th>Autor</th>
                        <th>ISBN</th>
                        <th>Año</th>
                        <th>Categoría</th>
                        <th>Disponible</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var libro in Model)
                    {
                        <tr>
                            <td>@libro.Titulo</td>
                            <td>@libro.Autor</td>
                            <td>@libro.ISBN</td>
                            <td>@libro.Anio</td>
                            <td><span class="badge bg-info">@libro.Categoria</span></td>
                            <td>
                                @if (libro.Disponible)
                                {
                                    <span class="badge bg-success">Sí</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">No</span>
                                }
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@libro.Id" class="btn btn-sm btn-warning">
                                    Editar
                                </a>
                                <a asp-action="Delete" asp-route-id="@libro.Id" class="btn btn-sm btn-danger">
                                    Eliminar
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
